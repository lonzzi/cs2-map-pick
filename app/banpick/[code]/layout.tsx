import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Ban/Pick 房间 - CS2 地图工具',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
