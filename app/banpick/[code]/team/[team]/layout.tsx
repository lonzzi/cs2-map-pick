import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: '队伍 Ban/Pick - CS2 地图工具',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
